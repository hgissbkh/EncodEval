eval_config:
  (): encodeval.eval_tasks.EvalConfig
  task_type: QA

  model_class: !ext transformers.AutoModelForTokenClassification
  model_kwargs:
    pretrained_model_name_or_path: ${EVAL_MODEL_PATH}
    num_labels: 2
    trust_remote_code: false
    attn_implementation: sdpa
    dtype: !ext torch.bfloat16
    device: cuda

  tokenizer_class: !ext transformers.AutoTokenizer
  tokenizer_kwargs:
    pretrained_model_name_or_path: ${EVAL_MODEL_PATH}
    trust_remote_code: false

  tr_args_class: !ext transformers.TrainingArguments
  tr_args_kwargs: 
    output_dir: /lus/home/CT10/cad16490/gisserotboukhlef/EncoderSL/EncoderScalingLaws-eval/results/main 
    output_subdir: lr5e-04_sd4
    do_train: true
    do_eval: true
    do_predict: true
    report_to: tensorboard
    train_batch_size: 32
    per_device_train_batch_size: 4
    per_device_eval_batch_size: 4
    learning_rate: 0.0005
    lr_scheduler_type: linear
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_epsilon: 0.00001
    warmup_ratio: 0.1
    weight_decay: 0.1
    num_train_epochs: 1
    max_steps: 1000
    seed: 4
    logging_steps: 10   
    fp16: false
    bf16: true
    eval_strategy: steps
    eval_steps: 100
    save_strategy: "no"

  max_length:
  load_dataset_from_custom_fn: !ext encodeval.datasets.squad_v2
